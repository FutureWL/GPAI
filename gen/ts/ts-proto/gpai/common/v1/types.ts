// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               unknown
// source: gpai/common/v1/types.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "gpai.common.v1";

export enum Market {
  MARKET_UNSPECIFIED = 0,
  MARKET_CN = 1,
  MARKET_HK = 2,
  MARKET_US = 3,
  UNRECOGNIZED = -1,
}

export function marketFromJSON(object: any): Market {
  switch (object) {
    case 0:
    case "MARKET_UNSPECIFIED":
      return Market.MARKET_UNSPECIFIED;
    case 1:
    case "MARKET_CN":
      return Market.MARKET_CN;
    case 2:
    case "MARKET_HK":
      return Market.MARKET_HK;
    case 3:
    case "MARKET_US":
      return Market.MARKET_US;
    case -1:
    case "UNRECOGNIZED":
    default:
      return Market.UNRECOGNIZED;
  }
}

export function marketToJSON(object: Market): string {
  switch (object) {
    case Market.MARKET_UNSPECIFIED:
      return "MARKET_UNSPECIFIED";
    case Market.MARKET_CN:
      return "MARKET_CN";
    case Market.MARKET_HK:
      return "MARKET_HK";
    case Market.MARKET_US:
      return "MARKET_US";
    case Market.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum AssetClass {
  ASSET_CLASS_UNSPECIFIED = 0,
  ASSET_CLASS_EQUITY = 1,
  ASSET_CLASS_ETF = 2,
  ASSET_CLASS_INDEX = 3,
  ASSET_CLASS_FUTURES = 4,
  ASSET_CLASS_OPTION = 5,
  ASSET_CLASS_CRYPTO = 6,
  UNRECOGNIZED = -1,
}

export function assetClassFromJSON(object: any): AssetClass {
  switch (object) {
    case 0:
    case "ASSET_CLASS_UNSPECIFIED":
      return AssetClass.ASSET_CLASS_UNSPECIFIED;
    case 1:
    case "ASSET_CLASS_EQUITY":
      return AssetClass.ASSET_CLASS_EQUITY;
    case 2:
    case "ASSET_CLASS_ETF":
      return AssetClass.ASSET_CLASS_ETF;
    case 3:
    case "ASSET_CLASS_INDEX":
      return AssetClass.ASSET_CLASS_INDEX;
    case 4:
    case "ASSET_CLASS_FUTURES":
      return AssetClass.ASSET_CLASS_FUTURES;
    case 5:
    case "ASSET_CLASS_OPTION":
      return AssetClass.ASSET_CLASS_OPTION;
    case 6:
    case "ASSET_CLASS_CRYPTO":
      return AssetClass.ASSET_CLASS_CRYPTO;
    case -1:
    case "UNRECOGNIZED":
    default:
      return AssetClass.UNRECOGNIZED;
  }
}

export function assetClassToJSON(object: AssetClass): string {
  switch (object) {
    case AssetClass.ASSET_CLASS_UNSPECIFIED:
      return "ASSET_CLASS_UNSPECIFIED";
    case AssetClass.ASSET_CLASS_EQUITY:
      return "ASSET_CLASS_EQUITY";
    case AssetClass.ASSET_CLASS_ETF:
      return "ASSET_CLASS_ETF";
    case AssetClass.ASSET_CLASS_INDEX:
      return "ASSET_CLASS_INDEX";
    case AssetClass.ASSET_CLASS_FUTURES:
      return "ASSET_CLASS_FUTURES";
    case AssetClass.ASSET_CLASS_OPTION:
      return "ASSET_CLASS_OPTION";
    case AssetClass.ASSET_CLASS_CRYPTO:
      return "ASSET_CLASS_CRYPTO";
    case AssetClass.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum Interval {
  INTERVAL_UNSPECIFIED = 0,
  INTERVAL_1M = 1,
  INTERVAL_5M = 2,
  INTERVAL_15M = 3,
  INTERVAL_1H = 4,
  INTERVAL_1D = 5,
  INTERVAL_1W = 6,
  INTERVAL_1MO = 7,
  UNRECOGNIZED = -1,
}

export function intervalFromJSON(object: any): Interval {
  switch (object) {
    case 0:
    case "INTERVAL_UNSPECIFIED":
      return Interval.INTERVAL_UNSPECIFIED;
    case 1:
    case "INTERVAL_1M":
      return Interval.INTERVAL_1M;
    case 2:
    case "INTERVAL_5M":
      return Interval.INTERVAL_5M;
    case 3:
    case "INTERVAL_15M":
      return Interval.INTERVAL_15M;
    case 4:
    case "INTERVAL_1H":
      return Interval.INTERVAL_1H;
    case 5:
    case "INTERVAL_1D":
      return Interval.INTERVAL_1D;
    case 6:
    case "INTERVAL_1W":
      return Interval.INTERVAL_1W;
    case 7:
    case "INTERVAL_1MO":
      return Interval.INTERVAL_1MO;
    case -1:
    case "UNRECOGNIZED":
    default:
      return Interval.UNRECOGNIZED;
  }
}

export function intervalToJSON(object: Interval): string {
  switch (object) {
    case Interval.INTERVAL_UNSPECIFIED:
      return "INTERVAL_UNSPECIFIED";
    case Interval.INTERVAL_1M:
      return "INTERVAL_1M";
    case Interval.INTERVAL_5M:
      return "INTERVAL_5M";
    case Interval.INTERVAL_15M:
      return "INTERVAL_15M";
    case Interval.INTERVAL_1H:
      return "INTERVAL_1H";
    case Interval.INTERVAL_1D:
      return "INTERVAL_1D";
    case Interval.INTERVAL_1W:
      return "INTERVAL_1W";
    case Interval.INTERVAL_1MO:
      return "INTERVAL_1MO";
    case Interval.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export interface Money {
  /** 最小单位(分/仙/美分) */
  amountMinor: number;
  /** ISO 4217 */
  currency: string;
}

function createBaseMoney(): Money {
  return { amountMinor: 0, currency: "" };
}

export const Money: MessageFns<Money> = {
  encode(message: Money, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.amountMinor !== 0) {
      writer.uint32(8).int64(message.amountMinor);
    }
    if (message.currency !== "") {
      writer.uint32(18).string(message.currency);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Money {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMoney();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.amountMinor = longToNumber(reader.int64());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.currency = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Money {
    return {
      amountMinor: isSet(object.amountMinor)
        ? globalThis.Number(object.amountMinor)
        : isSet(object.amount_minor)
        ? globalThis.Number(object.amount_minor)
        : 0,
      currency: isSet(object.currency) ? globalThis.String(object.currency) : "",
    };
  },

  toJSON(message: Money): unknown {
    const obj: any = {};
    if (message.amountMinor !== 0) {
      obj.amountMinor = Math.round(message.amountMinor);
    }
    if (message.currency !== "") {
      obj.currency = message.currency;
    }
    return obj;
  },

  create(base?: DeepPartial<Money>): Money {
    return Money.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Money>): Money {
    const message = createBaseMoney();
    message.amountMinor = object.amountMinor ?? 0;
    message.currency = object.currency ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
