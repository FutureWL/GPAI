// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               unknown
// source: gpai/market/v1/calendar.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "gpai.market.v1";

export interface TradingSession {
  /** "HH:MM" 24h */
  open: string;
  close: string;
}

export interface MarketCalendar {
  /** common.v1.Market 枚举值 */
  market: number;
  /** "YYYY-MM-DD" */
  date: string;
  isTradingDay: boolean;
  sessions: TradingSession[];
}

function createBaseTradingSession(): TradingSession {
  return { open: "", close: "" };
}

export const TradingSession: MessageFns<TradingSession> = {
  encode(
    message: TradingSession,
    writer: BinaryWriter = new BinaryWriter(),
  ): BinaryWriter {
    if (message.open !== "") {
      writer.uint32(10).string(message.open);
    }
    if (message.close !== "") {
      writer.uint32(18).string(message.close);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TradingSession {
    const reader =
      input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTradingSession();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.open = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.close = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TradingSession {
    return {
      open: isSet(object.open) ? globalThis.String(object.open) : "",
      close: isSet(object.close) ? globalThis.String(object.close) : "",
    };
  },

  toJSON(message: TradingSession): unknown {
    const obj: any = {};
    if (message.open !== "") {
      obj.open = message.open;
    }
    if (message.close !== "") {
      obj.close = message.close;
    }
    return obj;
  },

  create(base?: DeepPartial<TradingSession>): TradingSession {
    return TradingSession.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<TradingSession>): TradingSession {
    const message = createBaseTradingSession();
    message.open = object.open ?? "";
    message.close = object.close ?? "";
    return message;
  },
};

function createBaseMarketCalendar(): MarketCalendar {
  return { market: 0, date: "", isTradingDay: false, sessions: [] };
}

export const MarketCalendar: MessageFns<MarketCalendar> = {
  encode(
    message: MarketCalendar,
    writer: BinaryWriter = new BinaryWriter(),
  ): BinaryWriter {
    if (message.market !== 0) {
      writer.uint32(8).uint32(message.market);
    }
    if (message.date !== "") {
      writer.uint32(18).string(message.date);
    }
    if (message.isTradingDay !== false) {
      writer.uint32(24).bool(message.isTradingDay);
    }
    for (const v of message.sessions) {
      TradingSession.encode(v!, writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MarketCalendar {
    const reader =
      input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMarketCalendar();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.market = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.date = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.isTradingDay = reader.bool();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.sessions.push(TradingSession.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MarketCalendar {
    return {
      market: isSet(object.market) ? globalThis.Number(object.market) : 0,
      date: isSet(object.date) ? globalThis.String(object.date) : "",
      isTradingDay: isSet(object.isTradingDay)
        ? globalThis.Boolean(object.isTradingDay)
        : isSet(object.is_trading_day)
          ? globalThis.Boolean(object.is_trading_day)
          : false,
      sessions: globalThis.Array.isArray(object?.sessions)
        ? object.sessions.map((e: any) => TradingSession.fromJSON(e))
        : [],
    };
  },

  toJSON(message: MarketCalendar): unknown {
    const obj: any = {};
    if (message.market !== 0) {
      obj.market = Math.round(message.market);
    }
    if (message.date !== "") {
      obj.date = message.date;
    }
    if (message.isTradingDay !== false) {
      obj.isTradingDay = message.isTradingDay;
    }
    if (message.sessions?.length) {
      obj.sessions = message.sessions.map((e) => TradingSession.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<MarketCalendar>): MarketCalendar {
    return MarketCalendar.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MarketCalendar>): MarketCalendar {
    const message = createBaseMarketCalendar();
    message.market = object.market ?? 0;
    message.date = object.date ?? "";
    message.isTradingDay = object.isTradingDay ?? false;
    message.sessions =
      object.sessions?.map((e) => TradingSession.fromPartial(e)) || [];
    return message;
  },
};

type Builtin =
  | Date
  | Function
  | Uint8Array
  | string
  | number
  | boolean
  | undefined;

export type DeepPartial<T> = T extends Builtin
  ? T
  : T extends globalThis.Array<infer U>
    ? globalThis.Array<DeepPartial<U>>
    : T extends ReadonlyArray<infer U>
      ? ReadonlyArray<DeepPartial<U>>
      : T extends {}
        ? { [K in keyof T]?: DeepPartial<T[K]> }
        : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
