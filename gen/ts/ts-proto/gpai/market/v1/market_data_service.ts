// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               unknown
// source: gpai/market/v1/market_data_service.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { PageRequest, PageResponse } from "../../common/v1/pagination";
import { Market, marketFromJSON, marketToJSON } from "../../common/v1/types";
import { Instrument } from "../../instrument/v1/instrument";
import { Quote } from "./quote";

export const protobufPackage = "gpai.market.v1";

export interface GetQuoteRequest {
  instrumentId: string;
}

export interface GetQuoteResponse {
  quote: Quote | undefined;
}

export interface UpsertLatestQuoteRequest {
  quote: Quote | undefined;
}

export interface UpsertLatestQuoteResponse {
  accepted: boolean;
}

export interface ListInstrumentsRequest {
  market: Market;
  page: PageRequest | undefined;
}

export interface ListInstrumentsResponse {
  instruments: Instrument[];
  page: PageResponse | undefined;
}

function createBaseGetQuoteRequest(): GetQuoteRequest {
  return { instrumentId: "" };
}

export const GetQuoteRequest: MessageFns<GetQuoteRequest> = {
  encode(
    message: GetQuoteRequest,
    writer: BinaryWriter = new BinaryWriter(),
  ): BinaryWriter {
    if (message.instrumentId !== "") {
      writer.uint32(10).string(message.instrumentId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetQuoteRequest {
    const reader =
      input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetQuoteRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.instrumentId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetQuoteRequest {
    return {
      instrumentId: isSet(object.instrumentId)
        ? globalThis.String(object.instrumentId)
        : isSet(object.instrument_id)
          ? globalThis.String(object.instrument_id)
          : "",
    };
  },

  toJSON(message: GetQuoteRequest): unknown {
    const obj: any = {};
    if (message.instrumentId !== "") {
      obj.instrumentId = message.instrumentId;
    }
    return obj;
  },

  create(base?: DeepPartial<GetQuoteRequest>): GetQuoteRequest {
    return GetQuoteRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetQuoteRequest>): GetQuoteRequest {
    const message = createBaseGetQuoteRequest();
    message.instrumentId = object.instrumentId ?? "";
    return message;
  },
};

function createBaseGetQuoteResponse(): GetQuoteResponse {
  return { quote: undefined };
}

export const GetQuoteResponse: MessageFns<GetQuoteResponse> = {
  encode(
    message: GetQuoteResponse,
    writer: BinaryWriter = new BinaryWriter(),
  ): BinaryWriter {
    if (message.quote !== undefined) {
      Quote.encode(message.quote, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetQuoteResponse {
    const reader =
      input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetQuoteResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.quote = Quote.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetQuoteResponse {
    return {
      quote: isSet(object.quote) ? Quote.fromJSON(object.quote) : undefined,
    };
  },

  toJSON(message: GetQuoteResponse): unknown {
    const obj: any = {};
    if (message.quote !== undefined) {
      obj.quote = Quote.toJSON(message.quote);
    }
    return obj;
  },

  create(base?: DeepPartial<GetQuoteResponse>): GetQuoteResponse {
    return GetQuoteResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetQuoteResponse>): GetQuoteResponse {
    const message = createBaseGetQuoteResponse();
    message.quote =
      object.quote !== undefined && object.quote !== null
        ? Quote.fromPartial(object.quote)
        : undefined;
    return message;
  },
};

function createBaseUpsertLatestQuoteRequest(): UpsertLatestQuoteRequest {
  return { quote: undefined };
}

export const UpsertLatestQuoteRequest: MessageFns<UpsertLatestQuoteRequest> = {
  encode(
    message: UpsertLatestQuoteRequest,
    writer: BinaryWriter = new BinaryWriter(),
  ): BinaryWriter {
    if (message.quote !== undefined) {
      Quote.encode(message.quote, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(
    input: BinaryReader | Uint8Array,
    length?: number,
  ): UpsertLatestQuoteRequest {
    const reader =
      input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUpsertLatestQuoteRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.quote = Quote.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UpsertLatestQuoteRequest {
    return {
      quote: isSet(object.quote) ? Quote.fromJSON(object.quote) : undefined,
    };
  },

  toJSON(message: UpsertLatestQuoteRequest): unknown {
    const obj: any = {};
    if (message.quote !== undefined) {
      obj.quote = Quote.toJSON(message.quote);
    }
    return obj;
  },

  create(
    base?: DeepPartial<UpsertLatestQuoteRequest>,
  ): UpsertLatestQuoteRequest {
    return UpsertLatestQuoteRequest.fromPartial(base ?? {});
  },
  fromPartial(
    object: DeepPartial<UpsertLatestQuoteRequest>,
  ): UpsertLatestQuoteRequest {
    const message = createBaseUpsertLatestQuoteRequest();
    message.quote =
      object.quote !== undefined && object.quote !== null
        ? Quote.fromPartial(object.quote)
        : undefined;
    return message;
  },
};

function createBaseUpsertLatestQuoteResponse(): UpsertLatestQuoteResponse {
  return { accepted: false };
}

export const UpsertLatestQuoteResponse: MessageFns<UpsertLatestQuoteResponse> =
  {
    encode(
      message: UpsertLatestQuoteResponse,
      writer: BinaryWriter = new BinaryWriter(),
    ): BinaryWriter {
      if (message.accepted !== false) {
        writer.uint32(8).bool(message.accepted);
      }
      return writer;
    },

    decode(
      input: BinaryReader | Uint8Array,
      length?: number,
    ): UpsertLatestQuoteResponse {
      const reader =
        input instanceof BinaryReader ? input : new BinaryReader(input);
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseUpsertLatestQuoteResponse();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.accepted = reader.bool();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    },

    fromJSON(object: any): UpsertLatestQuoteResponse {
      return {
        accepted: isSet(object.accepted)
          ? globalThis.Boolean(object.accepted)
          : false,
      };
    },

    toJSON(message: UpsertLatestQuoteResponse): unknown {
      const obj: any = {};
      if (message.accepted !== false) {
        obj.accepted = message.accepted;
      }
      return obj;
    },

    create(
      base?: DeepPartial<UpsertLatestQuoteResponse>,
    ): UpsertLatestQuoteResponse {
      return UpsertLatestQuoteResponse.fromPartial(base ?? {});
    },
    fromPartial(
      object: DeepPartial<UpsertLatestQuoteResponse>,
    ): UpsertLatestQuoteResponse {
      const message = createBaseUpsertLatestQuoteResponse();
      message.accepted = object.accepted ?? false;
      return message;
    },
  };

function createBaseListInstrumentsRequest(): ListInstrumentsRequest {
  return { market: 0, page: undefined };
}

export const ListInstrumentsRequest: MessageFns<ListInstrumentsRequest> = {
  encode(
    message: ListInstrumentsRequest,
    writer: BinaryWriter = new BinaryWriter(),
  ): BinaryWriter {
    if (message.market !== 0) {
      writer.uint32(8).int32(message.market);
    }
    if (message.page !== undefined) {
      PageRequest.encode(message.page, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(
    input: BinaryReader | Uint8Array,
    length?: number,
  ): ListInstrumentsRequest {
    const reader =
      input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListInstrumentsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.market = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.page = PageRequest.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListInstrumentsRequest {
    return {
      market: isSet(object.market) ? marketFromJSON(object.market) : 0,
      page: isSet(object.page) ? PageRequest.fromJSON(object.page) : undefined,
    };
  },

  toJSON(message: ListInstrumentsRequest): unknown {
    const obj: any = {};
    if (message.market !== 0) {
      obj.market = marketToJSON(message.market);
    }
    if (message.page !== undefined) {
      obj.page = PageRequest.toJSON(message.page);
    }
    return obj;
  },

  create(base?: DeepPartial<ListInstrumentsRequest>): ListInstrumentsRequest {
    return ListInstrumentsRequest.fromPartial(base ?? {});
  },
  fromPartial(
    object: DeepPartial<ListInstrumentsRequest>,
  ): ListInstrumentsRequest {
    const message = createBaseListInstrumentsRequest();
    message.market = object.market ?? 0;
    message.page =
      object.page !== undefined && object.page !== null
        ? PageRequest.fromPartial(object.page)
        : undefined;
    return message;
  },
};

function createBaseListInstrumentsResponse(): ListInstrumentsResponse {
  return { instruments: [], page: undefined };
}

export const ListInstrumentsResponse: MessageFns<ListInstrumentsResponse> = {
  encode(
    message: ListInstrumentsResponse,
    writer: BinaryWriter = new BinaryWriter(),
  ): BinaryWriter {
    for (const v of message.instruments) {
      Instrument.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.page !== undefined) {
      PageResponse.encode(message.page, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(
    input: BinaryReader | Uint8Array,
    length?: number,
  ): ListInstrumentsResponse {
    const reader =
      input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListInstrumentsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.instruments.push(Instrument.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.page = PageResponse.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListInstrumentsResponse {
    return {
      instruments: globalThis.Array.isArray(object?.instruments)
        ? object.instruments.map((e: any) => Instrument.fromJSON(e))
        : [],
      page: isSet(object.page) ? PageResponse.fromJSON(object.page) : undefined,
    };
  },

  toJSON(message: ListInstrumentsResponse): unknown {
    const obj: any = {};
    if (message.instruments?.length) {
      obj.instruments = message.instruments.map((e) => Instrument.toJSON(e));
    }
    if (message.page !== undefined) {
      obj.page = PageResponse.toJSON(message.page);
    }
    return obj;
  },

  create(base?: DeepPartial<ListInstrumentsResponse>): ListInstrumentsResponse {
    return ListInstrumentsResponse.fromPartial(base ?? {});
  },
  fromPartial(
    object: DeepPartial<ListInstrumentsResponse>,
  ): ListInstrumentsResponse {
    const message = createBaseListInstrumentsResponse();
    message.instruments =
      object.instruments?.map((e) => Instrument.fromPartial(e)) || [];
    message.page =
      object.page !== undefined && object.page !== null
        ? PageResponse.fromPartial(object.page)
        : undefined;
    return message;
  },
};

export type MarketDataServiceDefinition = typeof MarketDataServiceDefinition;
export const MarketDataServiceDefinition = {
  name: "MarketDataService",
  fullName: "gpai.market.v1.MarketDataService",
  methods: {
    getQuote: {
      name: "GetQuote",
      requestType: GetQuoteRequest as typeof GetQuoteRequest,
      requestStream: false,
      responseType: GetQuoteResponse as typeof GetQuoteResponse,
      responseStream: false,
      options: {},
    },
    upsertLatestQuote: {
      name: "UpsertLatestQuote",
      requestType: UpsertLatestQuoteRequest as typeof UpsertLatestQuoteRequest,
      requestStream: false,
      responseType:
        UpsertLatestQuoteResponse as typeof UpsertLatestQuoteResponse,
      responseStream: false,
      options: {},
    },
    listInstruments: {
      name: "ListInstruments",
      requestType: ListInstrumentsRequest as typeof ListInstrumentsRequest,
      requestStream: false,
      responseType: ListInstrumentsResponse as typeof ListInstrumentsResponse,
      responseStream: false,
      options: {},
    },
  },
} as const;

type Builtin =
  | Date
  | Function
  | Uint8Array
  | string
  | number
  | boolean
  | undefined;

export type DeepPartial<T> = T extends Builtin
  ? T
  : T extends globalThis.Array<infer U>
    ? globalThis.Array<DeepPartial<U>>
    : T extends ReadonlyArray<infer U>
      ? ReadonlyArray<DeepPartial<U>>
      : T extends {}
        ? { [K in keyof T]?: DeepPartial<T[K]> }
        : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
